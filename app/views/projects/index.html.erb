<div class="container col-md-8 offset-md-2 topper">
  <div class="container">
    <p id="notice"><%= notice %></p>

    <div id="latest-blogpost">
      <h3 class="lede">Latest</h3>
      <hr>

      <div class="row featurette">
        <div class="col-lg-8">
          <%= link_to "/projects/#{@projects[@count-1].id}" do %>
            <%= image_tag @projects[@count - 1].img, alt: @projects[@count - 1].title, class:"img-fluid mx-auto latest-post-img"  %>
          <% end %>
        </div>
        <div class="col-lg">
          <%= link_to "/projects/#{@projects[@count-1].id}" do %>
            <h2 class="headline"><%= @projects[@count - 1].title %></h2>
            <h4 class="lede"><%= @projects[@count - 1].subtitle %></h4>
            <p>
              <span class="leed author">
                <% if @projects[@count-1].updated_at > @projects[@count-1].created_at %>
                  Updated <%= @projects[@count - 1].updated_at.to_s[5,5] %>
                <% else %>
                  <%= @projects[@count - 1].created_at.to_s[5,5] %>
                <% end %>
              </span>
            </p>
          <% end %>
        </div>
      </div>
    </div>



    <% if @projects.count >= 4 %>
    <div id="next-three-blogposts" class="row featurette topper">
      <% 3.times do |i| %>
        <% count = @count - 2 - i %>
        <div class="col-lg">
          <%= link_to "/projects/#{@projects[count].id}" do %>
            <%= image_tag @projects[count].img, alt: @projects[count].title, class:"img-fluid mx-auto blog-post-img"  %>
            <h4 class="headline"><%= @projects[count].title %></h4>
            <h6 class="lede"><%= @projects[count].subtitle %></h6>
            <p>
              <span class="lede author">
                <% if @projects[count].updated_at > @projects[count].created_at %>
                  Updated <%= @projects[count].updated_at.to_s[5,5] %>
                <% else %>
                  <%= @projects[count].created_at.to_s[5,5] %>
                <% end %>
              </span>
            </p>
          <% end %>
        </div>
      <% end %>
    </div>
    <% end %>

    <% if @projects.count >= 7 %>
    <div id="next-three-blogposts" class="row featurette">
      <% 3.times do |i| %>
        <% count = @count - 5 - i %>
        <div class="col-lg">
          <%= link_to "/projects/#{@projects[count].id}" do %>
            <%= image_tag @projects[count].img, alt: @projects[count].title, class:"img-fluid mx-auto blog-post-img"  %>
            <h4 class="headline"><%= @projects[count].title %></h4>
            <h6 class="lede"><%= @projects[count].subtitle %></h6>
            <p>
              <span class="lede author">
                <% if @projects[count].updated_at > @projects[count].created_at %>
                  Updated <%= @projects[count].updated_at.to_s[5,5] %>
                <% else %>
                  <%= @projects[count].created_at.to_s[5,5] %>
                <% end %>
              </span>
            </p>
          <% end %>
        </div>
      <% end %>
    </div>
    <% end %>
  </div>

  <hr class="featurette-divider">

  <div class="container">
    <%= link_to 'New project', new_project_path if logged_in?(:site_admin) %>
  </div>
</div>
