<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :blog_title %>
  </div>

  <div class="field">
    <%= form.label :subtitle %>
    <%= form.text_field :subtitle, id: :blog_subtitle %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text, id: :blog_text %>
  </div>

  <div class="field">
    <%= form.label :img %>
    <%= form.text_area :img, id: :blog_img %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
